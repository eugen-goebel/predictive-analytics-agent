# Runtime dependencies for the Streamlit Cloud demo.
# pyproject.toml + uv.lock stay the source of truth for local dev and CI.
# This is the subset the dashboard (app.py) needs at runtime. openpyxl is
# included because the profiler reads .xlsx uploads via pandas.read_excel,
# and python-docx because the orchestrator writes the DOCX report.
streamlit>=1.62.0
scikit-learn>=1.9.0
pandas>=3.0.5
numpy>=2.5.2; python_version >= "3.12"
numpy>=2.4.6,<2.5.0; python_version < "3.12"
matplotlib>=3.11.1
python-docx>=1.2.0
pydantic>=2.13.4
openpyxl>=3.1.5
